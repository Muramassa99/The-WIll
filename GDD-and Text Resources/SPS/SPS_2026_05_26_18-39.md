got # SPS - Save Project State

## Current Loaded Worldspace
- Active slice: Stage 1 V2 weapon crafter / forge V2.
- Mental focus: make V2 a real 3D material authoring workflow, not a plane-bound block editor.
- Current architecture direction:
  - Workspace Contract defines the legal authoring space.
  - Placement Target Resolver answers what the cursor is aiming at.
  - Brush strokes receive local 3D points and should survive future targeting changes.
- The current placement plane is temporary. It still provides a target today, but it is no longer intended to be the foundation.
- User goal behind this slice: make Stage 1 V2 usable enough to create better weapons for later Skill Crafter testing.

## Previous SPS Plan
- No previous SPS found in `GDD-and Text Resources/SPS`.
- This is the first SPS file.

## Done Since Last SPS
- Created V2 workspace foundation:
  - `runtime/forge_v2/forge_v2_workspace_contract.gd`
  - Owns local bounds, placement mode, current plane depth, grid step, fit size, visible bounds box flag, stroke sample spacing, and future world prop anchor id.
- Routed V2 workspace preview through the contract:
  - `runtime/forge_v2/forge_v2_workspace_preview.gd`
  - Plane, grid, axes, bounds, fit view, and local clamping now come from the contract.
- Added continuous brush stroke placement:
  - `runtime/forge_v2/forge_v2_authoring_state.gd`
  - `runtime/forge_v2/forge_v2_stage_controller.gd`
  - `runtime/forge_v2/crafting_bench_ui_v2.gd`
  - Left click still places a single point.
  - Left click and drag now creates one multi-point capsule-path material body instead of many unrelated dots.
  - Stroke sampling spacing is contract-driven.
- Improved V2 Shape menu increments:
  - `runtime/forge_v2/crafting_bench_ui_v2.gd`
  - Radius and Amount controls are inline in the Shape menu.
  - Shape menu stays open for repeated Radius/Amount clicks, copied from V1 menu behavior.
- Added Placement Target Resolver V1:
  - `runtime/forge_v2/forge_v2_placement_target_resolver.gd`
  - `runtime/forge_v2/forge_v2_workspace_preview.gd`
  - Current target kind is `target_placement_plane`.
  - Resolver returns richer hit data: `valid`, `target_kind`, local/world position, raw/clamped position, local/world normal, hit distance, source body id, source record id, ray origin, ray direction.
  - `screen_to_workspace_local()` remains as compatibility wrapper.
- Updated project memory:
  - `GDD-and Text Resources/ToDo stuff/forge_v2_design_notes.md`
  - Added notes for Workspace Contract, Brush Stroke Placement Foundation, Placement Target Resolver V1, and diegetic world-space authoring box.
- Verification done during the slice:
  - V2 UI scene loaded cleanly after each major change.
  - Temporary verifier confirmed drag stroke becomes one material body with multi-point path.
  - Temporary verifier confirmed Shape menu increment behavior.
  - Temporary verifier confirmed resolver center ray hits `target_placement_plane`.
  - Temporary verifier files and Godot logs were cleaned.
- Known unrelated warning during full project load:
  - `user://skills/player_skill_slot_state.tres` cannot save.
  - This existed before the V2 changes and was not part of this slice.
- No git commit or push was made.

## Planned Points Hit
- No previous SPS plan existed.
- Active chat plan hit:
  - Build Workspace Contract foundation.
  - Add continuous brush stroke placement.
  - Copy V1 repeated increment/menu behavior into V2.
  - Add Placement Target Resolver V1 so the plane becomes one target mode rather than core architecture.

## Planned Points Missed
- No previous SPS plan existed.
- Not reached yet:
  - `target_workspace_box_face`
  - `target_material_surface`
  - Real transparent authoring box as a placement target.
  - In-viewport material body selection by clicking geometry.
  - Deleting material bodies directly from viewport context/right-click.
  - Replacing the plane fully.

## New Current State
- V2 is testable at a basic level:
  - Open V2 crafting bench UI.
  - Left click places a blob.
  - Left drag paints a connected tube-like stroke.
  - Radius/Amount can be adjusted from Shape menu with repeated clicks.
- V2 placement still visually uses an authoring plane, grid, axes, and bounds box.
- Architecturally, placement is no longer hardwired directly into `ForgeV2WorkspacePreview`; the preview delegates target resolution.
- The next targeting upgrade should happen in the resolver, not in the UI brush code.
- Important current files:
  - `runtime/forge_v2/forge_v2_placement_target_resolver.gd`
  - `runtime/forge_v2/forge_v2_workspace_contract.gd`
  - `runtime/forge_v2/forge_v2_workspace_preview.gd`
  - `runtime/forge_v2/crafting_bench_ui_v2.gd`
  - `runtime/forge_v2/forge_v2_authoring_state.gd`
  - `runtime/forge_v2/forge_v2_stage_controller.gd`
  - `runtime/forge_v2/forge_v2_volume_preview_presenter.gd`
  - `GDD-and Text Resources/ToDo stuff/forge_v2_design_notes.md`

## Next Focus
- Add `target_workspace_box_face` to `forge_v2_placement_target_resolver.gd`.
- Keep it narrow:
  - Ray should hit the legal authoring box faces.
  - Return `target_kind = target_workspace_box_face`.
  - Return face normal and clamped local hit position.
  - Do not solve material-surface targeting yet.
  - Do not remove the plane yet; keep plane as fallback/current mode until box targeting is verified.

## First Click / First Action
- Open:
  - `C:\WORKSPACE\The Will- main folder\the-will-gamefiles\runtime\forge_v2\forge_v2_placement_target_resolver.gd`
- Thought anchor:
  - Add a box-face ray intersection path beside the existing plane path.
  - The resolver should choose target mode from `workspace_contract.placement_mode`.
  - The brush stroke system should not change for this step.

## Do Not Load Yet
- Do not deep-load Skill Crafter unless V2 export/weapon handoff becomes the immediate blocker.
- Do not deep-load Stage 2 refinement yet; only use it for small reference if needed.
- Do not return to V1 spline deletion yet.
- Do not work on chase materials, disassembly, blueprint upgrading, or material rarity rules yet.
- Do not solve armor, ranged skill crafter, shield skill crafter, or engraving station yet.
- Do not touch git commit/push unless explicitly requested.
